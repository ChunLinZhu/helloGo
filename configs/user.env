# User Service 配置（gRPC 微服务）
APP_ENV=development
GRPC_PORT=50001

# 数据库（使用独立 SQLite 文件，避免与单体应用并发锁冲突）
DB_TYPE=sqlite
SQLITE_PATH=./data/user.db

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASS=11111

# JWT
JWT_SECRET=dev-secret-key-do-not-use-in-production
