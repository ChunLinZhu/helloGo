apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: {{ .Values.namespace }}
  labels:
    app: auth-service
spec:
  replicas: {{ .Values.services.auth.replicas }}
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: auth-service
          image: "{{ .Values.services.auth.image }}:{{ .Values.services.auth.tag }}"
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          ports:
            - name: grpc
              containerPort: {{ .Values.services.auth.grpcPort }}
            - name: health
              containerPort: {{ .Values.services.auth.healthPort }}
          env:
            {{- include "hellogo.env" . | nindent 12 }}
            - name: GRPC_PORT
              value: "{{ .Values.services.auth.grpcPort }}"
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 10
          resources:
            requests:
              cpu: {{ .Values.services.auth.resources.requests.cpu }}
              memory: {{ .Values.services.auth.resources.requests.memory }}
            limits:
              cpu: {{ .Values.services.auth.resources.limits.cpu }}
              memory: {{ .Values.services.auth.resources.limits.memory }}
